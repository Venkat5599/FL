# Uniswap feedback has moved

The canonical Uniswap developer feedback for KOLlateral now lives at the repository root:

**[../FEEDBACK.md](../FEEDBACK.md)**

The early integration notes that used to be here (the load-bearing `Accept` header, the UniswapX `protocols` default and its order minimum, the Permit2 two-step flow, and the `check_approval` proxy) have been folded into that file, so there is a single source of truth for the audit.
